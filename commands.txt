git init// used to nitialize a repository
gti status // used to check the status
git add .// used to add or track all files
git commit -m "some message "// to commit
git commit -a// tracks and commits all modified ot already added files
git config --global user.name = "1rn21cs018alan"
git config --global user.email =  "1rn21cs018alangeorgejimcy@gmail.com"

